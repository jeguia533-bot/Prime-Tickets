# Prime Tickets

Railway start command:

```bash
python bot.py
```

Railway variable needed:

```env
DISCORD_TOKEN=your_token_here
```

Developer Portal:
- Server Members Intent ON
- Message Content Intent ON
- Invite with `bot` and `applications.commands`
